# 기능 상세 스펙 문서 - 09. 타입 시스템

> **문서 버전**: 2.2  
> **작성일**: 2025-12-25  
> **업데이트**: 2025-12-25 - 번역 관련 인터페이스 및 통합 타입 정의
> **담당 모듈**: `src/types/index.ts`

---

## 1. AI 및 서비스 관련 타입

### 1.1 Task 및 Provider
```typescript
export type AIProvider = 'gemini' | 'openai' | 'claude';
export type AITask = 'tone-conversion' | 'translation';
```

### 1.2 번역 응답 인터페이스
```typescript
export interface TranslationResponse {
  success: boolean;
  translatedText: string;
  detectedLanguage: string;
  error?: string;
}
```

---

## 2. Storage 관련 타입 (v2.2 확장)

### 2.1 TranslationSettings
```typescript
export interface TranslationSettings {
  defaultTargetLanguage: 'ko' | 'en' | 'ja' | 'zh-CN';
  preserveLineBreaks: boolean;
  showNotification: boolean;
}
```

### 2.2 AppStorage 개편
```typescript
export interface AppStorage {
  apiKeys: Record<AIProvider, string>;
  settings: {
    selectedProvider: AIProvider;
    providerModels: Record<AIProvider, string>; // 제공자별 선택 모델 추가 ⭐
    autoCopyEnabled: boolean;
    autoCopyTone: ToneType;
    translation: TranslationSettings;
  };
  lastUsedTab: 'tone' | 'translation';
}
```

---

## 3. UI 및 상태 관리 타입

### 3.1 Popup Tab State
```typescript
export type TabId = 'tone' | 'translation';
```

---

**문서 작성**: BCA 개발팀  
**최종 업데이트**: 2025-12-25

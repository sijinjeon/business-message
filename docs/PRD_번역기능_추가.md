# PRD: 번역 기능 추가 (Translation Feature)

> **문서 버전**: 2.1  
> **작성일**: 2025-12-25  
> **업데이트**: 2025-12-25 - 간소화된 번역 기능으로 재설계
> **기존 버전**: 1.0 (정중한 문장 도우미)  
> **추가 기능**: AI 기반 실시간 번역 기능

---

## 목차

1. [개요](#1-개요)
2. [목표](#2-목표)
3. [기능 명세](#3-기능-명세)
4. [사용자 인터페이스](#4-사용자-인터페이스)
5. [단축키 시스템](#5-단축키-시스템)
6. [번역 전문가 모드](#6-번역-전문가-모드)
7. [언어 감지 및 처리](#7-언어-감지-및-처리)
8. [설정 관리](#8-설정-관리)
9. [기술 요구사항](#9-기술-요구사항)
10. [사용자 플로우](#10-사용자-플로우)
11. [성공 지표](#11-성공-지표)

---

## 1. 개요

### 1.1 제품 진화

**기존 제품 (v1.0)**:
- 이름: 정중한 문장 도우미
- 기능: 한국 직장 문화에 맞는 톤 변환 (Formal, General, Friendly)

**새 제품 (v2.0)**:
- 이름: **비즈니스 커뮤니케이션 어시스턴트**
- 기능: 
  1. ✅ 정중한 문장 변환 (기존)
  2. 🆕 **AI 기반 전문 번역** (신규)

### 1.2 추가 기능 개요

**번역 기능**은 사무직, IT, 생산성 분야의 전문가들이 다양한 언어로 작성된 컨텐츠를 빠르고 정확하게 번역할 수 있도록 지원합니다.

**핵심 차별점**:
- 🔍 **자동 언어 감지**: 어떤 언어든 자동 인식
- ⚡ **즉시 번역**: 텍스트 선택 → `Cmd+I` → 자동 번역
- 📝 **형식 유지**: 줄바꿈, 문단 구조 완벽 보존
- 🎯 **스마트 타겟팅**: 설정한 언어로 자동 번역 (한/영/일/중)

---

## 2. 목표

### 2.1 비즈니스 목표

**핵심 목표**:
- v2.0 출시 3개월 내 번역 기능 사용률 70% 달성
- 일일 활성 사용자(DAU) 1,000명 → 3,000명 증가
- 사용자 평균 세션 시간 2배 증가

**확장 목표**:
- 글로벌 시장 진출 기반 마련
- 프리미엄 기능 차별화 요소 확보

### 2.2 사용자 목표

**타겟 사용자**:
1. **개발자**: GitHub, Stack Overflow, 기술 문서 번역
2. **비즈니스 전문가**: 이메일, 보고서, 계약서 번역
3. **연구자/학생**: 논문, 학술 자료 번역
4. **콘텐츠 크리에이터**: SNS, 블로그 게시물 번역
5. **게이머**: 게임 채팅, 공략 번역

**사용자가 해결하려는 문제**:
- ❌ 번역하려면 복사 → 번역기 열기 → 붙여넣기 → 복사 → 다시 붙여넣기 (번거로움)
- ❌ 번역 후 줄바꿈이 깨지거나 형식이 망가짐
- ❌ 어떤 언어인지 확인하고 수동으로 선택해야 함
- ❌ 웹페이지를 벗어나야 번역 가능

**우리의 솔루션**:
- ✅ 텍스트 선택 → `Cmd+I` → 즉시 번역 (2초 안에 완료)
- ✅ 줄바꿈, 문단 구조 완벽 유지
- ✅ 언어 자동 감지 (한국어/영어/일본어/중국어)
- ✅ 어떤 웹페이지에서든 바로 번역 가능

---

## 3. 기능 명세

### 3.1 기능 모드 구분

| 모드 | 단축키 | 기능 | 우선순위 |
|------|--------|------|----------|
| **정중한 문장 모드** | `Cmd+Option+C` (Mac)<br>`Ctrl+Alt+C` (Win) | 한국어 톤 변환<br>(Formal/General/Friendly) | P0 (기존) |
| **번역 모드** | `Cmd+I` (Mac/Win 공통) | 선택된 텍스트를 설정한 언어로 즉시 번역 | P0 (신규) |

### 3.2 번역 기능 상세

#### 3.2.1 지원 언어

**타겟 언어** (설정에서 선택 가능):
1. 🇰🇷 한국어 (Korean)
2. 🇺🇸 영어 (English)
3. 🇯🇵 일본어 (Japanese)
4. 🇨🇳 중국어 간체 (Simplified Chinese)

**소스 언어** (자동 감지):
- ✅ 한국어, 영어, 일본어, 중국어 자동 인식
- ✅ 기타 주요 언어도 감지 가능 (스페인어, 프랑스어, 독일어 등)
- ✅ AI가 자동으로 언어를 판단하여 타겟 언어로 번역

**기본 설정**:
- 소스: 자동 감지 (사용자가 선택할 필요 없음)
- 타겟: 한국어 (설정에서 변경 가능)

#### 3.2.2 텍스트 길이 및 형식 지원

| 유형 | 예시 | 처리 방식 |
|------|------|-----------|
| **단어** | "productivity", "bug" | 문맥 기반 번역 |
| **짧은 문장** | "Let me know if you need help." | 자연스러운 번역 |
| **여러 문장 (줄바꿈 포함)** | "안녕하세요.\n전시진입니다.\n컨설턴트를 하고 있습니다." | **줄바꿈 위치 그대로 유지하며 번역** |
| **문단** | 이메일, 보고서 본문 | 문맥 유지 + 줄바꿈 보존 |
| **긴 글** | 블로그 게시물, 문서 | 전체 일관성 유지 + 형식 보존 |

**형식 유지 규칙** ⭐:
- ✅ 줄바꿈(`\n`) 위치 정확히 보존
- ✅ 빈 줄도 그대로 유지
- ✅ 문단 구조 유지
- ✅ 들여쓰기 보존 (가능한 경우)

**제한사항**:
- 최대 길이: 5,000자 (AI API 토큰 제한 고려)
- 초과 시: 앞 5,000자만 번역 + 경고 메시지

---

## 4. 사용자 인터페이스

### 4.1 번역 기능 UX 개요

번역 기능은 **별도 UI 없이 바로 동작**합니다:

1. 사용자가 웹페이지/문서에서 텍스트 선택
2. `Cmd+I` (Mac) 또는 `Ctrl+I` (Win/Linux) 단축키 입력
3. 선택된 텍스트가 자동으로 번역되어 **즉시 대체**됨
4. 번역 완료 시 간단한 토스트 알림 표시

```
[웹페이지 - 번역 전]
┌─────────────────────────────────────────┐
│ Hello.                                  │
│ I'm Sijin Jeon.                         │  ← 사용자가 이 부분 선택
│ I work as a consultant.                 │
└─────────────────────────────────────────┘
         ↓ Cmd+I 입력
         ↓ (1-2초 대기)
[웹페이지 - 번역 후]
┌─────────────────────────────────────────┐
│ 안녕하세요.                              │
│ 전시진입니다.                            │  ← 번역되어 대체됨
│ 컨설턴트를 하고 있습니다.                │
└─────────────────────────────────────────┘
         ↓
[토스트 알림] ✅ 영어 → 한국어 번역 완료
```

### 4.2 설정 UI (Options 페이지)

설정 페이지에 번역 관련 설정 추가:

```
┌─────────────────────────────────────────────────┐
│  🌐 번역 설정                                    │
├─────────────────────────────────────────────────┤
│                                                 │
│  번역 타겟 언어                                 │
│  ┌───────────────────────────────────────────┐  │
│  │ 🇰🇷 한국어                            ▼  │  │
│  └───────────────────────────────────────────┘  │
│  • 🇰🇷 한국어 (Korean)                          │
│  • 🇺🇸 영어 (English)                           │
│  • 🇯🇵 일본어 (Japanese)                        │
│  • 🇨🇳 중국어 간체 (Simplified Chinese)         │
│                                                 │
│  ℹ️ Cmd+I로 텍스트 선택 시 이 언어로 번역됩니다 │
│                                                 │
│  ── 고급 설정 ─────────────────────────────────  │
│  ☑️ 줄바꿈 형식 유지                             │
│  ☑️ 번역 완료 알림 표시                          │
│  최대 텍스트 길이: [5000] 자                    │
│                                                 │
│  [저장]  [초기화]                               │
└─────────────────────────────────────────────────┘
```

### 4.3 토스트 알림

번역 완료 시 화면 우측 하단에 간단한 알림:

```
┌──────────────────────────────┐
│ ✅ 번역 완료                  │
│ 영어 → 한국어                │
│ (3줄, 1.2초 소요)            │
└──────────────────────────────┘
```

오류 발생 시:
```
┌──────────────────────────────┐
│ ❌ 번역 실패                  │
│ API 오류 또는 네트워크 문제   │
│ [다시 시도]                  │
└──────────────────────────────┘
```

### 4.4 정중한 문장 모드 UI (기존 유지)

기존 팝업 UI는 그대로 유지:
- `Cmd+Option+C`로 팝업 열기
- 3가지 톤 선택하여 변환

---

## 5. 단축키 시스템

### 5.1 단축키 정의

| OS | 정중한 문장 모드 | 번역 모드 |
|----|-----------------|-----------|
| **macOS** | `Cmd+Option+C` | `Cmd+I` |
| **Windows** | `Ctrl+Alt+C` | `Ctrl+I` |
| **Linux** | `Ctrl+Alt+C` | `Ctrl+I` |

### 5.2 단축키 동작

**Chrome Commands API + Content Script**

```json
// manifest.json
{
  "commands": {
    "tone-conversion": {
      "suggested_key": {
        "default": "Ctrl+Alt+C",
        "mac": "Command+Alt+C"
      },
      "description": "정중한 문장으로 변환"
    },
    "instant-translation": {
      "suggested_key": {
        "default": "Ctrl+I",
        "mac": "Command+I"
      },
      "description": "선택한 텍스트 즉시 번역"
    }
  }
}
```

**번역 모드 동작 플로우**:
```
1. 사용자가 웹페이지에서 텍스트 선택
   예: "Hello.\nI'm Sijin.\nI work as a consultant."
   ↓
2. Cmd+I 입력
   ↓
3. Content Script가 선택된 텍스트 감지
   window.getSelection().toString()
   ↓
4. Background Script로 텍스트 전송
   ↓
5. 언어 자동 감지 (영어 감지됨)
   ↓
6. 설정된 타겟 언어로 API 호출 (예: 한국어)
   - 줄바꿈(\n) 위치 저장
   - AI에게 번역 요청 (줄바꿈 유지 지시)
   ↓
7. 번역 결과 받기
   "안녕하세요.\n전시진입니다.\n컨설턴트를 하고 있습니다."
   ↓
8. Content Script가 선택된 텍스트를 번역으로 대체
   document.execCommand('insertText', false, translatedText)
   또는 contentEditable 영역의 경우 직접 대체
   ↓
9. 토스트 알림 표시
   "✅ 영어 → 한국어 번역 완료 (3줄, 1.2초)"
```

**정중한 문장 모드 동작 플로우** (기존 유지):
```
1. 사용자가 텍스트 복사 (Cmd+C)
   ↓
2. Cmd+Option+C 입력
   ↓
3. 팝업 열기 → 클립보드 읽기
   ↓
4. 3가지 톤으로 변환 표시
```

### 5.3 단축키 충돌 처리

**`Cmd+I` 충돌 가능성**:
- 일부 웹사이트에서 `Cmd+I`는 이탤릭 서식
- Chrome 확장 프로그램의 단축키가 우선순위를 가짐
- 사용자가 원하면 `chrome://extensions/shortcuts`에서 변경 가능

**대안 단축키** (사용자 요청 시):
- `Cmd+Shift+T`: Translate
- `Cmd+Shift+I`: Instant translate
- `Alt+T`: 간단한 조합

설정 페이지에 안내:
```
┌─────────────────────────────────────────────────┐
│  ⌨️ 단축키 설정                                  │
│                                                 │
│  정중한 문장: Cmd+Option+C                      │
│  즉시 번역: Cmd+I                               │
│                                                 │
│  ℹ️ Cmd+I가 다른 기능과 충돌하면               │
│     Chrome 단축키 설정에서 변경할 수 있습니다   │
│                                                 │
│  [Chrome 단축키 설정 열기 →]                    │
└─────────────────────────────────────────────────┘
```

---

## 6. 번역 품질 및 프롬프트 전략

### 6.1 번역 프롬프트 기본 구조

**단일 범용 번역 프롬프트** ⭐

v2.1에서는 복잡한 전문가 모드 대신, 하나의 고품질 범용 번역 프롬프트를 사용합니다.

```
# Role
당신은 전문 번역가입니다. 자연스럽고 정확한 번역을 제공합니다.

# Task
다음 텍스트를 {{target_language}}(으)로 번역하세요.

# Critical Rules
1. **줄바꿈 완벽 유지**: 원문의 모든 줄바꿈(\n) 위치를 정확히 보존하세요.
2. **자연스러운 번역**: 직역이 아닌 의미를 전달하는 번역
3. **일관성 유지**: 동일한 용어는 일관되게 번역
4. **문맥 이해**: 전체 맥락을 고려한 번역
5. **형식 보존**: 빈 줄, 들여쓰기 등 모든 형식 유지

# Example
원문:
Hello.
I'm Sijin.
I work as a consultant.

번역 (한국어):
안녕하세요.
전시진입니다.
컨설턴트를 하고 있습니다.

# Original Text
{{user_input}}

# Target Language
{{target_language}}

# Important
- 반드시 번역된 텍스트만 출력하세요
- 설명이나 부연 설명 없이 번역 결과만 제공
- 줄바꿈 위치를 원문과 정확히 동일하게 유지
```

### 6.2 줄바꿈 처리 전략

**문제**: AI가 줄바꿈을 무시하고 하나의 문단으로 합칠 수 있음

**해결책 1: 명시적 지시**
```typescript
const prompt = `
다음 텍스트를 번역하세요.
중요: 각 줄의 줄바꿈(\n)을 정확히 유지하세요.

원문:
${text}

번역 (${targetLanguage}):
`
```

**해결책 2: 줄 단위 번역 후 조합**
```typescript
// 1. 텍스트를 줄 단위로 분리
const lines = text.split('\n')

// 2. 각 줄을 개별 번역 (병렬 처리 가능)
const translatedLines = await Promise.all(
  lines.map(line => translateLine(line, targetLanguage))
)

// 3. 줄바꿈으로 재조합
const result = translatedLines.join('\n')
```

**해결책 3: 구조화된 응답 요청**
```typescript
const prompt = `
Translate each line separately and return as JSON array.

Input:
["Hello.", "I'm Sijin.", "I work as a consultant."]

Output format:
["안녕하세요.", "전시진입니다.", "컨설턴트를 하고 있습니다."]

Input:
${JSON.stringify(lines)}
`
```

### 6.3 언어별 번역 특성

**한국어 ↔ 영어**:
- 존댓말 자동 감지 및 유지
- 영어의 간결함 vs 한국어의 정중함
- 주어 생략 처리

**한국어 ↔ 일본어**:
- 존경어 레벨 매칭
- 한자어 대응
- 문화적 표현 번역

**한국어 ↔ 중국어**:
- 간체/번체 구분
- 한자 공통 뿌리 활용
- 어순 차이 처리

**영어 ↔ 일본어**:
- 가타카나 외래어 처리
- 경어 레벨 선택
- 문화적 맥락 추가

---

## 7. 언어 감지 및 처리

### 7.1 언어 자동 감지

**방법**: AI에게 언어 감지 요청

```typescript
// 첫 번째 API 호출: 언어 감지
const detectedLanguage = await detectLanguage(inputText)

// 두 번째 API 호출: 번역
const translation = await translate(inputText, detectedLanguage, targetLanguage, expertMode)
```

**최적화**: 한 번의 API 호출로 처리

```typescript
// 프롬프트에 언어 감지 + 번역 통합
const prompt = `
Detect the language of the following text and translate it to ${targetLanguage}.

Original Text:
${inputText}

Output Format (JSON):
{
  "detected_language": "en",
  "detected_language_name": "English",
  "translation": "..."
}
`
```

### 7.2 언어 감지 UI 표시

```
┌─────────────────────────────────────────────────┐
│  원문                                           │
│  ┌───────────────────────────────────────────┐  │
│  │ Hello, world!                             │  │
│  └───────────────────────────────────────────┘  │
│  [🇺🇸 영어 감지됨] ← 자동 감지 결과            │
│                                                 │
│  타겟 언어: [🇰🇷 한국어 ▼]                      │
│  자동 변환: 영어 → 한국어 ✅                    │
└─────────────────────────────────────────────────┘
```

### 7.3 단어 vs 문장 처리

**단일 단어**:
```
원문: "productivity"
번역: "생산성"

원문: "bug"
번역: "버그" (기술 문맥) 또는 "벌레" (일반 문맥)
→ AI가 주변 문맥이 없으므로 가장 일반적인 의미로 번역
```

**여러 문장 (줄바꿈 포함)**:
```
원문:
"Hello.
I'm Sijin.
I work as a consultant."

번역:
"안녕하세요.
전시진입니다.
컨설턴트를 하고 있습니다."
```

### 7.4 언어 감지 최적화

**빠른 감지 전략**:
```typescript
// 간단한 휴리스틱으로 먼저 체크
function quickDetectLanguage(text: string): string | null {
  // 한글 체크
  if (/[ㄱ-ㅎ|ㅏ-ㅣ|가-힣]/.test(text)) return 'ko'
  
  // 일본어 체크
  if (/[\u3040-\u309F|\u30A0-\u30FF]/.test(text)) return 'ja'
  
  // 중국어 체크
  if (/[\u4E00-\u9FFF]/.test(text)) return 'zh'
  
  // 영어/기타는 AI에게 위임
  return null
}

// AI 호출은 필요한 경우만
const detectedLang = quickDetectLanguage(text) || await aiDetectLanguage(text)
```

---

## 8. 설정 관리

### 8.1 번역 설정 데이터 스키마

```typescript
interface TranslationSettings {
  // 기본 설정
  defaultTargetLanguage: 'ko' | 'en' | 'ja' | 'zh-CN';  // 기본: 'ko'
  
  // 기능 설정
  preserveLineBreaks: boolean;  // 기본: true (줄바꿈 유지)
  showNotification: boolean;    // 기본: true (완료 알림)
  
  // 고급 설정
  maxTextLength: number;  // 기본: 5000
  autoDetectLanguage: boolean;  // 기본: true (항상 켜짐)
}
```

### 8.2 설정 UI

**Settings 페이지 - 새 섹션 추가**:

```
┌─────────────────────────────────────────────────┐
│  설정                                           │
├─────────────────────────────────────────────────┤
│  [AI 제공업체 설정] (기존)                      │
│  [정중한 문장 설정] (기존)                      │
│  [번역 설정] (신규) ⭐                          │
└─────────────────────────────────────────────────┘
```

**번역 설정 상세** (이미 섹션 4.2에 표시됨):

```
┌─────────────────────────────────────────────────┐
│  🌐 번역 설정                                    │
├─────────────────────────────────────────────────┤
│                                                 │
│  번역 타겟 언어                                 │
│  ┌───────────────────────────────────────────┐  │
│  │ 🇰🇷 한국어                            ▼  │  │
│  └───────────────────────────────────────────┘  │
│  • 🇰🇷 한국어 (Korean)                          │
│  • 🇺🇸 영어 (English)                           │
│  • 🇯🇵 일본어 (Japanese)                        │
│  • 🇨🇳 중국어 간체 (Simplified Chinese)         │
│                                                 │
│  ℹ️ Cmd+I로 텍스트 선택 시 이 언어로 번역됩니다 │
│                                                 │
│  ── 기능 설정 ─────────────────────────────────  │
│  ☑️ 줄바꿈 형식 유지 (권장)                      │
│  ☑️ 번역 완료 알림 표시                          │
│                                                 │
│  ── 고급 설정 ─────────────────────────────────  │
│  최대 텍스트 길이: [5000] 자                    │
│  ☑️ 언어 자동 감지 (항상 켜짐)                   │
│                                                 │
│  [저장]  [초기화]                               │
└─────────────────────────────────────────────────┘
```

### 8.3 Storage 스키마 업데이트

```typescript
// 기존 AppStorage 확장
interface AppStorage {
  // 기존 필드들...
  apiKeys: {
    gemini?: string;
    openai?: string;
    anthropic?: string;
  };
  dailyUsage: {
    date: string;
    count: number;
  };
  settings: {
    // 기존 정중한 문장 설정
    selectedProvider: AIProvider;
    autoCopyEnabled: boolean;
    autoCopyTone: 'formal' | 'general' | 'friendly';
    
    // 신규 번역 설정
    translation: TranslationSettings;  // 위에서 정의한 인터페이스
  };
  
  // 번역 통계 (선택 사항)
  translationStats?: {
    totalTranslations: number;
    lastUsed: string;
    languagePairs: {
      [key: string]: number;  // 예: "en->ko": 15
    };
  };
}
```

---

## 9. 기술 요구사항

### 9.1 manifest.json 업데이트

```json
{
  "manifest_version": 3,
  "name": "비즈니스 커뮤니케이션 어시스턴트",
  "version": "2.1.0",
  "description": "정중한 문장 변환 + 즉시 번역",
  
  "permissions": [
    "storage",
    "clipboardRead",
    "clipboardWrite",
    "activeTab"
  ],
  
  "content_scripts": [
    {
      "matches": ["<all_urls>"],
      "js": ["content-script.js"],
      "run_at": "document_idle"
    }
  ],
  
  "commands": {
    "tone-conversion": {
      "suggested_key": {
        "default": "Ctrl+Alt+C",
        "mac": "Command+Alt+C"
      },
      "description": "정중한 문장으로 변환"
    },
    "instant-translation": {
      "suggested_key": {
        "default": "Ctrl+I",
        "mac": "Command+I"
      },
      "description": "선택한 텍스트 즉시 번역"
    }
  },
  
  "host_permissions": [
    "https://generativelanguage.googleapis.com/*",
    "https://api.openai.com/*",
    "https://api.anthropic.com/*"
  ]
}
```

### 9.2 API 프롬프트 구조

```typescript
interface TranslationRequest {
  text: string;
  targetLanguage: 'ko' | 'en' | 'ja' | 'zh-CN';
  preserveLineBreaks: boolean;  // 기본: true
}

interface TranslationResponse {
  success: boolean;
  detectedLanguage: string;        // 예: 'en'
  detectedLanguageName: string;    // 예: 'English'
  translatedText: string;
  processingTime?: number;         // 밀리초
  error?: string;
}
```

**API 호출 예시**:

```typescript
async function translateText(
  text: string,
  targetLanguage: string,
  provider: AIProvider,
  apiKey: string
): Promise<TranslationResponse> {
  const prompt = buildTranslationPrompt(text, targetLanguage)
  
  try {
    const response = await callAI(provider, apiKey, prompt)
    
    return {
      success: true,
      detectedLanguage: detectLanguageFromText(text),
      detectedLanguageName: getLanguageName(detectedLanguage),
      translatedText: response.text,
      processingTime: response.time
    }
  } catch (error) {
    return {
      success: false,
      detectedLanguage: '',
      detectedLanguageName: '',
      translatedText: '',
      error: error.message
    }
  }
}
```

### 9.3 새 유틸리티 함수

```typescript
// src/utils/translation.ts (신규)

/**
 * 텍스트를 타겟 언어로 번역
 */
export async function translateText(
  text: string,
  targetLanguage: 'ko' | 'en' | 'ja' | 'zh-CN',
  provider: AIProvider,
  apiKey: string
): Promise<TranslationResponse>

/**
 * 언어 빠른 감지 (휴리스틱)
 */
export function quickDetectLanguage(text: string): string | null {
  if (/[ㄱ-ㅎ|ㅏ-ㅣ|가-힣]/.test(text)) return 'ko'
  if (/[\u3040-\u309F|\u30A0-\u30FF]/.test(text)) return 'ja'
  if (/[\u4E00-\u9FFF]/.test(text)) return 'zh'
  return null  // 영어 또는 기타
}

/**
 * 번역 프롬프트 생성
 */
export function buildTranslationPrompt(
  text: string,
  targetLanguage: string
): string {
  const languageNames = {
    ko: '한국어',
    en: 'English',
    ja: '日本語',
    'zh-CN': '简体中文'
  }
  
  return `
# Role
당신은 전문 번역가입니다.

# Critical Rules
1. 줄바꿈(\\n) 위치를 정확히 유지하세요
2. 자연스럽고 정확한 번역을 제공하세요
3. 번역된 텍스트만 출력하세요 (설명 없이)

# Original Text
${text}

# Target Language
${languageNames[targetLanguage]}

# Translation
`
}

/**
 * 언어 코드를 언어 이름으로 변환
 */
export function getLanguageName(code: string): string {
  const names = {
    ko: '한국어',
    en: 'English',
    ja: '日本語',
    zh: '中文',
    'zh-CN': '简体中文'
  }
  return names[code] || code
}

/**
 * 선택된 텍스트 가져오기
 */
export function getSelectedText(): string {
  return window.getSelection()?.toString() || ''
}

/**
 * 선택된 텍스트를 번역으로 대체
 */
export function replaceSelectedText(translatedText: string): void {
  const selection = window.getSelection()
  if (!selection || selection.rangeCount === 0) return
  
  const range = selection.getRangeAt(0)
  range.deleteContents()
  range.insertNode(document.createTextNode(translatedText))
}
```

### 9.4 컴포넌트 및 파일 구조

```
src/
├── popup/                         # 기존 팝업 (정중한 문장 모드)
│   ├── App.tsx
│   ├── components/
│   │   ├── TextInput.tsx
│   │   ├── ResultCard.tsx
│   │   └── ActionBar.tsx
│   └── index.tsx
│
├── content/                       # 신규: Content Script
│   ├── content-script.ts          # 메인 content script
│   ├── text-replacer.ts           # 텍스트 대체 로직
│   └── toast.ts                   # 토스트 알림
│
├── background/                    # Background Service Worker
│   ├── background.ts              # 메인 background script
│   ├── commands.ts                # 단축키 핸들러
│   └── translation-handler.ts     # 번역 처리
│
├── options/                       # 설정 페이지
│   ├── Settings.tsx               # 메인 설정
│   └── components/
│       └── TranslationSettings.tsx # 번역 설정 컴포넌트
│
├── utils/
│   ├── api.ts                     # 기존 (확장)
│   ├── translation.ts             # 신규: 번역 유틸
│   ├── storage.ts                 # 스토리지 (스키마 확장)
│   └── clipboard.ts               # 기존
│
└── types/
    └── index.ts                   # Translation 타입 추가
```

**새로 추가되는 파일들**:

1. `src/content/content-script.ts`: 웹페이지에 주입되어 텍스트 선택 감지 및 대체
2. `src/content/text-replacer.ts`: 선택된 텍스트를 번역으로 안전하게 대체
3. `src/content/toast.ts`: 번역 완료/오류 알림 표시
4. `src/background/translation-handler.ts`: 번역 API 호출 및 응답 처리
5. `src/utils/translation.ts`: 번역 관련 유틸리티 함수들

---

## 10. 사용자 플로우

### 10.1 기본 번역 플로우 (즉시 번역)

**시나리오 1: 영어 → 한국어**

```
1. 웹페이지에서 영어 텍스트 선택 (드래그)
   ┌─────────────────────────────┐
   │ Hello.                      │ ← 선택
   │ I'm Sijin Jeon.             │ ← 선택
   │ I work as a consultant.     │ ← 선택
   └─────────────────────────────┘
   ↓
2. Cmd+I 입력 (Mac) 또는 Ctrl+I (Win)
   ↓
3. [로딩] 번역 중... (1-2초)
   - Content Script: 선택된 텍스트 감지
   - Background: 언어 감지 (영어)
   - API: 한국어로 번역 요청
   ↓
4. 선택된 텍스트가 번역으로 즉시 대체됨
   ┌─────────────────────────────┐
   │ 안녕하세요.                  │
   │ 전시진입니다.                │
   │ 컨설턴트를 하고 있습니다.     │
   └─────────────────────────────┘
   ↓
5. 토스트 알림 표시 (우측 하단)
   ┌──────────────────────────┐
   │ ✅ 번역 완료              │
   │ 영어 → 한국어            │
   │ (3줄, 1.2초)             │
   └──────────────────────────┘
   (3초 후 자동 사라짐)
```

**시나리오 2: 한국어 → 영어**

```
1. 설정에서 타겟 언어를 "영어"로 변경
   (또는 기본값이 영어로 설정된 경우)
   ↓
2. 웹페이지에서 한국어 텍스트 선택
   "안녕하세요.\n전시진입니다."
   ↓
3. Cmd+I 입력
   ↓
4. 번역 완료 (1-2초)
   "Hello.\nI'm Sijin Jeon."
   ↓
5. 토스트: "✅ 한국어 → 영어"
```

### 10.2 설정 변경 플로우

```
1. 확장 프로그램 아이콘 클릭
   ↓
2. [⚙️ 설정] 버튼 클릭
   ↓
3. 설정 페이지 열림
   ↓
4. "🌐 번역 설정" 섹션으로 스크롤
   ↓
5. 타겟 언어 변경
   [🇰🇷 한국어 ▼] → [🇺🇸 영어]
   ↓
6. [저장] 버튼 클릭
   ↓
7. "✅ 설정이 저장되었습니다" 메시지
   ↓
8. 이제 Cmd+I로 영어로 번역됨
```

### 10.3 오류 처리 플로우

```
1. 텍스트 선택 후 Cmd+I
   ↓
2. API 오류 발생 (네트워크, 키 오류 등)
   ↓
3. 토스트 알림 표시
   ┌──────────────────────────────┐
   │ ❌ 번역 실패                  │
   │ API 키를 확인해주세요         │
   │ [설정 열기] [다시 시도]      │
   └──────────────────────────────┘
   ↓
4. [설정 열기] 클릭
   → 설정 페이지의 AI 제공업체 섹션으로 이동
```

### 10.4 단어 번역 플로우

```
1. 단일 단어 선택
   "productivity" ← 선택
   ↓
2. Cmd+I
   ↓
3. 번역 (0.5초)
   "생산성"
   ↓
4. 토스트: "✅ 영어 → 한국어 (단어)"
```

### 10.5 긴 문서 번역 플로우

```
1. 문단 전체 선택 (여러 줄)
   ↓
2. Cmd+I
   ↓
3. 길이 체크
   - 5,000자 이하: 정상 번역
   - 5,000자 초과: 경고 알림
     "⚠️ 텍스트가 너무 깁니다 (6,234자)
      앞 5,000자만 번역됩니다"
   ↓
4. 번역 진행
   ↓
5. 완료 (2-3초)
```

---

## 11. 성공 지표

### 11.1 핵심 지표 (KPI)

| 구분 | 지표 | 현재 (v1.0) | 목표 (v2.0, 3개월 후) |
|------|------|-------------|------------------------|
| **사용자** | DAU | 1,000명 | 3,000명 (+200%) |
| **사용자** | 주간 활성 사용자 (WAU) | 5,000명 | 15,000명 |
| **사용자** | 평균 세션 시간 | 30초 | 60초 (+100%) |
| **기능** | 번역 기능 사용률 | 0% | 70% |
| **기능** | 전문가 모드 사용 분포 | - | 상위 5개 모드가 80% |
| **품질** | 번역 만족도 (설문) | - | 4.5/5.0 |
| **품질** | 재번역률 | - | < 20% |
| **성능** | 번역 API 응답 시간 | - | < 2초 |
| **비즈니스** | Chrome 웹스토어 평점 | 4.5/5.0 | 4.7/5.0 |

### 11.2 추적 이벤트

```typescript
// Analytics Events (선택 사항)

// 번역 사용
track('instant_translation_used', {
  source_language: 'en',
  target_language: 'ko',
  text_length: 150,
  line_count: 3,
  processing_time: 1.2  // 초
})

// 번역 성공
track('translation_success', {
  source_language: 'en',
  target_language: 'ko',
  processing_time: 1.2
})

// 번역 실패
track('translation_failed', {
  error_type: 'api_error' | 'network_error' | 'invalid_key',
  error_message: string
})

// 설정 변경
track('translation_settings_updated', {
  default_target_language: 'ko',
  preserve_line_breaks: true,
  show_notification: true
})

// 단축키 사용
track('shortcut_used', {
  command: 'instant-translation',
  selected_text_length: 150
})
```

### 11.3 A/B 테스트 계획

**테스트 1: 토스트 알림 스타일**
- A: 최소한의 정보 (✅ 번역 완료)
- B: 상세 정보 (✅ 영어 → 한국어, 1.2초)
- 측정: 사용자 만족도, 알림 유용성

**테스트 2: 단축키 조합**
- A: `Cmd+I` (권장)
- B: `Cmd+Shift+T`
- C: `Alt+T`
- 측정: 단축키 충돌 빈도, 사용자 선호도

**테스트 3: 줄바꿈 처리 방식**
- A: AI에게 명시적 지시
- B: 줄 단위 번역 후 조합
- 측정: 번역 품질, 줄바꿈 보존 정확도, 처리 속도

---

## 12. 개발 우선순위 및 일정

### 12.1 Phase 1: Core Translation Feature (2-3주)

**Week 1: 기반 구축**
- [ ] Content Script 구현
  - [ ] 텍스트 선택 감지
  - [ ] 선택된 텍스트 대체 로직
  - [ ] 토스트 알림 UI
- [ ] Background Script 업데이트
  - [ ] `instant-translation` 단축키 핸들러
  - [ ] Content Script와 통신
- [ ] Translation Utils
  - [ ] `translateText()` 함수
  - [ ] `quickDetectLanguage()` 함수
  - [ ] `buildTranslationPrompt()` 함수
  - [ ] 줄바꿈 보존 로직

**Week 2: 통합 및 설정**
- [ ] manifest.json 업데이트
  - [ ] Content scripts 추가
  - [ ] `instant-translation` 명령어 추가
  - [ ] 권한 업데이트 (`activeTab`)
- [ ] Storage 스키마 확장
  - [ ] `TranslationSettings` 추가
  - [ ] 마이그레이션 로직
- [ ] Settings UI
  - [ ] 번역 설정 섹션 추가
  - [ ] 타겟 언어 선택기
  - [ ] 옵션 토글들

**Week 3: 테스트 및 최적화**
- [ ] 다양한 웹사이트 테스트
  - [ ] contentEditable 영역
  - [ ] textarea 요소
  - [ ] 일반 텍스트 선택
- [ ] 오류 처리 개선
  - [ ] API 오류
  - [ ] 네트워크 오류
  - [ ] 길이 제한 경고
- [ ] 성능 최적화
  - [ ] API 응답 시간 개선
  - [ ] 줄바꿈 처리 최적화
- [ ] 문서 업데이트
  - [ ] README
  - [ ] 사용자 가이드

### 12.2 Phase 2: 개선 및 확장 (1-2주)

**선택적 기능**:
- [ ] 번역 히스토리
  - [ ] 최근 번역 5개 저장
  - [ ] 팝업에서 히스토리 보기
- [ ] 번역 통계
  - [ ] 사용량 추적
  - [ ] 언어 쌍 통계
- [ ] 고급 옵션
  - [ ] 번역 품질 설정 (빠름/정확함)
  - [ ] 캐싱 (동일 텍스트 재번역 방지)
- [ ] 추가 언어 지원
  - [ ] 스페인어, 프랑스어, 독일어 등

### 12.3 Phase 3: 출시 및 모니터링

- [ ] Chrome 웹스토어 제출
  - [ ] 설명 업데이트
  - [ ] 스크린샷 추가 (번역 기능)
  - [ ] v2.1 출시
- [ ] 사용자 피드백 수집
- [ ] 버그 수정 및 개선
- [ ] A/B 테스트 실행

---

## 부록 A: 기술 구현 세부사항

### A.1 Content Script 구현 예시

```typescript
// src/content/content-script.ts

// 메시지 리스너 등록
chrome.runtime.onMessage.addListener((request, sender, sendResponse) => {
  if (request.action === 'getSelectedText') {
    const selectedText = window.getSelection()?.toString() || ''
    sendResponse({ text: selectedText })
  }
  
  if (request.action === 'replaceText') {
    replaceSelectedText(request.translatedText)
    showToast(request.notification)
    sendResponse({ success: true })
  }
  
  return true
})

function replaceSelectedText(newText: string) {
  const selection = window.getSelection()
  if (!selection || selection.rangeCount === 0) return
  
  const range = selection.getRangeAt(0)
  range.deleteContents()
  range.insertNode(document.createTextNode(newText))
}

function showToast(message: string) {
  const toast = document.createElement('div')
  toast.className = 'translation-toast'
  toast.textContent = message
  document.body.appendChild(toast)
  
  setTimeout(() => toast.remove(), 3000)
}
```

### A.2 Background Script 구현 예시

```typescript
// src/background/commands.ts

chrome.commands.onCommand.addListener(async (command) => {
  if (command === 'instant-translation') {
    const [tab] = await chrome.tabs.query({ active: true, currentWindow: true })
    if (!tab.id) return
    
    // 1. 선택된 텍스트 가져오기
    const response = await chrome.tabs.sendMessage(tab.id, { 
      action: 'getSelectedText' 
    })
    
    if (!response.text) {
      showNotification('텍스트를 선택해주세요')
      return
    }
    
    // 2. 설정 로드
    const settings = await loadTranslationSettings()
    
    // 3. 번역 수행
    const result = await translateText(
      response.text,
      settings.defaultTargetLanguage,
      settings.selectedProvider,
      settings.apiKey
    )
    
    if (!result.success) {
      showNotification(`❌ 번역 실패: ${result.error}`)
      return
    }
    
    // 4. 텍스트 대체
    await chrome.tabs.sendMessage(tab.id, {
      action: 'replaceText',
      translatedText: result.translatedText,
      notification: `✅ ${result.detectedLanguageName} → ${getLanguageName(settings.defaultTargetLanguage)}`
    })
  }
})
```

### A.3 줄바꿈 보존 전략 비교

| 방식 | 장점 | 단점 | 권장 |
|------|------|------|------|
| **AI 명시적 지시** | 간단, 빠름 | 100% 보장 어려움 | ⭐ MVP |
| **줄 단위 번역** | 100% 보존 | 병렬 API 호출 필요, 문맥 손실 | Phase 2 |
| **JSON 구조화** | 정확함 | 프롬프트 복잡, 토큰 낭비 | 선택 사항 |

---

## 부록 B: 예상 질문 (FAQ)

**Q1: 기존 정중한 문장 기능에 영향이 있나요?**
A: 없습니다. 기존 `Cmd+Option+C` 단축키와 팝업 UI는 그대로 유지됩니다.

**Q2: 모든 웹사이트에서 작동하나요?**
A: 대부분의 웹사이트에서 작동하지만, 일부 보안이 강화된 사이트(은행, 정부 사이트)에서는 Content Script가 차단될 수 있습니다.

**Q3: 번역 품질은 어느 정도인가요?**
A: Google Gemini/OpenAI API를 사용하므로 Google 번역보다 자연스럽고 문맥을 고려한 번역이 제공됩니다.

**Q4: 오프라인에서도 작동하나요?**
A: 아니오. AI API 호출이 필요하므로 인터넷 연결이 필수입니다.

**Q5: API 비용은 얼마나 드나요?**
A: 
- Google Gemini: 무료 할당량 내에서는 무료
- OpenAI: 텍스트 길이에 따라 다름 (평균 $0.0001~0.001/번역)

---

**문서 작성자**: Product Manager  
**검토자**: CTO, Lead Developer  
**최종 승인**: CEO  
**버전 히스토리**: 
- v2.0 (2025-12-25): 초안 작성 (복잡한 전문가 모드 시스템)
- v2.1 (2025-12-25): 단순화된 즉시 번역 기능으로 재설계
**다음 단계**: 개발 시작 → Phase 1 구현


# 기능 상세 스펙 문서 - 09. 타입 시스템

> **문서 버전**: 1.0  
> **작성일**: 2025-12-25  
> **담당 모듈**: `src/types/index.ts`

---

## 목차

1. [개요](#1-개요)
2. [AI 관련 타입](#2-ai-관련-타입)
3. [Storage 관련 타입](#3-storage-관련-타입)
4. [컴포넌트 Props 타입](#4-컴포넌트-props-타입)
5. [상태 관리 타입](#5-상태-관리-타입)

---

## 1. 개요

**파일**: `src/types/index.ts` (107 lines)

**목적**: 전체 애플리케이션의 TypeScript 타입 정의를 중앙 관리

---

## 2. AI 관련 타입

### 2.1 AIProvider

```typescript
export type AIProvider = 'gemini' | 'chatgpt' | 'claude';
```

**용도**: AI 제공업체 식별

### 2.2 AIApiResponse

```typescript
export interface AIApiResponse {
  formal: string;      // 비즈니스 이메일 톤
  general: string;     // 사내 메신저 톤
  friendly: string;    // 캐주얼 채팅 톤
}
```

### 2.3 API 요청 타입

**Gemini**:
```typescript
export interface GeminiApiRequest {
  contents: Array<{
    parts: Array<{
      text: string;
    }>;
  }>;
  generationConfig: {
    temperature: number;
    maxOutputTokens: number;
    candidateCount: number;
    topK: number;
    topP: number;
  };
  safetySettings: Array<{
    category: string;
    threshold: string;
  }>;
}
```

**OpenAI**:
```typescript
export interface OpenAIApiRequest {
  model: string;
  messages: Array<{
    role: 'system' | 'user' | 'assistant';
    content: string;
  }>;
  temperature: number;
  max_tokens: number;
}
```

**Claude**:
```typescript
export interface ClaudeApiRequest {
  model: string;
  max_tokens: number;
  temperature: number;
  messages: Array<{
    role: 'user' | 'assistant';
    content: string;
  }>;
}
```

---

## 3. Storage 관련 타입

### 3.1 AppStorage

```typescript
export interface AppStorage {
  apiKeys: {
    gemini: string;
    chatgpt: string;
    claude: string;
  };
  dailyUsage: {
    date: string;
    count: number;
  };
  settings: {
    selectedProvider: AIProvider;
    preferredModel: string;
    temperature: number;
    maxOutputTokens: number;
    autoCopyEnabled: boolean;
    autoCopyTone: 'formal' | 'general' | 'friendly';
  };
}
```

---

## 4. 컴포넌트 Props 타입

### 4.1 ResultCardProps

```typescript
export interface ResultCardProps {
  tone: 'formal' | 'general' | 'friendly';
  text: string;
  onCopy: (text: string) => void;
  isDefaultSelected?: boolean;
}
```

### 4.2 TextInputProps

```typescript
export interface TextInputProps {
  value: string;
  onChange: (value: string) => void;
  maxLength: number;
  placeholder: string;
}
```

### 4.3 ActionBarProps

```typescript
export interface ActionBarProps {
  onRegenerate: () => void;
  onReadClipboard: () => void;
  isLoading: boolean;
  onOpenSettings: () => void;
}
```

---

## 5. 상태 관리 타입

### 5.1 LoadingState

```typescript
export type LoadingState = 'idle' | 'loading' | 'success' | 'error';
```

### 5.2 AppState

```typescript
export interface AppState {
  inputText: string;
  results: AIApiResponse | null;
  loadingState: LoadingState;
  message: string;
  messageType: 'error' | 'success' | '';
  remainingUsage: number;
}
```

---

**문서 작성**: AI 코드 분석 시스템  
**최종 업데이트**: 2025-12-25

